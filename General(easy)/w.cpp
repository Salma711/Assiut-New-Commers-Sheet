#include <iostream>
#include <cmath>
#include <algorithm>
using namespace std;
#define ll long long
#define salma ios_base ::sync_with_stdio(false); cin.tie(NULL);

int main()
{
    salma;
    string a, b, c;
    cin >> a >> b;
    c.empty() ? cout << -1 : cout << c;
}